
<div class="game">
	
	<div class="soundSlider">

	<% for sound in @: %>

	<div data-id="<%= sound.name %>" class="audioBox">
		<img src="<%= window.game.path %>/images/soundIcon.png"/>
			<audio preload="auto" class="<%= sound.name %>" src="<%= window.game.path %>/sounds/<%= sound.name %>.mp3" type="audio/mpeg"></audio>
		
	</div>

	<% end %>

	</div>

	<div class="machineSlider">
		
	<% for sound in @: %>

			<div data-id="<%= sound.name %>" class="imageBox">
				<img src="<%= window.game.path %>/images/<%= sound.name %>.png"/>
			</div>

		<% end %>

	</div>	


</div>